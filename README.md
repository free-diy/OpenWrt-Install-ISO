## 使用方式
1. 生成的ISO镜像同时支持物理机和虚拟机的安装
2. 虚拟机使用：各种虚拟机启动直接选择iso即可
3. 物理机使用：建议将iso放入ventoy的U盘中启动
4. 启动安装器系统，命令行输入 `ddd` 命令即可调出安装菜单，然后按提示操作
5. 自定义openwrt镜像生成iso安装器,其中openwrt镜像的压缩包格式是`img.gz` `img.zip` `img.xz`三种
