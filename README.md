## 使用方式
1. 虚拟机使用：各种虚拟机直接选择iso即可
2. 物理机使用：建议将iso放入ventoy的U盘中
3. 具体的操作方法是:在安装器所在系统里输入 `ddd` 命令即可调出安装菜单
4. 生成的ISO同时 支持物理机 和 虚拟机的安装
5. 支持自定义openwrt镜像生成iso安装器,其中openwrt镜像的压缩包格式是`img.gz` `img.zip` `img.xz`三种
